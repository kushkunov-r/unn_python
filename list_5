list=input("Введите список чисел разделенных пробелом: ").split()
for i in range(len(list)):
    list[i]=int(list[i])
for i in list:
    if i==0:
        list.remove(i)
        list.append(i)
print(list)
